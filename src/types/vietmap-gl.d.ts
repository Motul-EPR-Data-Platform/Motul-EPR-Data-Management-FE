// Type declarations for @vietmap/vietmap-gl-js internal dependencies
declare module '@mapbox/point-geometry' {
  export default class Point {
    x: number;
    y: number;
    constructor(x: number, y: number);
  }
}

